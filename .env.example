# =============================================================================
# Environment Variables Configuration
# =============================================================================
# 
# This file documents all environment variables used in the application.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit .env, .env.production, or .env.test to Git!
#
# Variable Types:
# - PUBLIC_* prefix: Available in client-side code (safe for browser)
# - No prefix: Server-side only (API routes, middleware)
#
# =============================================================================

# -----------------------------------------------------------------------------
# Environment Name
# -----------------------------------------------------------------------------
# Options: local | integration | prod
# Used for feature flags and environment-specific behavior
ENV_NAME=local

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Database and authentication service
# URL and anon key are safe to expose to clients (RLS protects data)
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here

# Service role key: NEVER expose to client! Server-side only.
# Has admin access - used for server operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# -----------------------------------------------------------------------------
# AI Service (OpenRouter)
# -----------------------------------------------------------------------------
# API key for AI flashcard generation
# Server-side only - never exposed to client
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Mock AI generation for testing (true/false)
# Set to "true" to use mock data instead of real AI API calls
MOCK_AI_GENERATION=false

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# PostgreSQL password (if using direct database access)
POSTGRESS_PSW=your_postgres_password_here

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
# Optional: Pre-authenticated token for local development
DEV_AUTH_TOKEN=

# Optional: E2E test credentials
E2E_USERNAME_ID=
E2E_USERNAME=
E2E_PASSWORD=

# Optional: Mailtrap for E2E email testing
MAILTRAP_HOST=
MAILTRAP_PORT=
MAILTRAP_USERNAME=
MAILTRAP_PASSWORD=
MAILTRAP_API_TOKEN=
MAILTRAP_INBOX_ID=
MAILTRAP_ACCOUNT_ID=
